spring:
  config:
    activate:
      on-profile: dev

workflow:
  runtime:
    environment: development
  agent:
    provider:
      egress:
        allow-private-networks: true
        allow-http: true
  auth:
    token:
      cookie-secure: false

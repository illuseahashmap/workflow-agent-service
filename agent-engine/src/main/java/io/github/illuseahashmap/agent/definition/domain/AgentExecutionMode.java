package io.github.illuseahashmap.agent.definition.domain;

/** Stable dispatch key for selecting an Agent executor. */
public enum AgentExecutionMode {
    MODEL_ONLY,
    PLATFORM_AGENT,
    REMOTE_AGENT
}

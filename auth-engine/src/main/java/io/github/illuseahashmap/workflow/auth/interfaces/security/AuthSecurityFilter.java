package io.github.illuseahashmap.workflow.auth.interfaces.security;

import jakarta.servlet.Filter;

/** Public HTTP-security adapter contract exposed by auth-engine to the application host. */
public interface AuthSecurityFilter extends Filter {
}
